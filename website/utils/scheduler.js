export function leitnerSchedule(result) {
    const nextDueDate = None
    if (result.totalItems == 0) {
        
    } else if (result.totalItems == 1) {

    } else {

    }
    return nextDueDate
    // if 0 total items -> tomorrow
    // if 1 total items -> in 2 days
    // else: diff
    // Identify how many items
    // calc difference
    // return lastDue + twice difference
}